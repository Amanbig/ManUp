# typescript-eslint's published peerDependencies range still caps at
# `typescript <6.1.0`, but this project runs typescript@^7.x. The parser
# works fine against TS 7 in practice; this just stops npm from treating
# that stale peer range as a hard install failure.
legacy-peer-deps=true
